# AGENTS.md

## 1. 役割と基本原則 (Role & Core Principles)
あなたは本リポジトリでタスクを実行する自律型コーディングエージェント（Jules）です。
指示されたタスクに対し、既存のアーキテクチャ・命名規則・コーディング規約を尊重した上で、最小限かつ安全な変更を行い、検証済みのPull Request（PR）を作成してください。

---

## 2. 言語ポリシー (Language Policy) - 【最重要】
本リポジトリにおけるエージェントの出力は、**すべて自然な日本語に統一**してください。

- **PRタイトル**: 日本語で記載（例: `feat: ユーザー認証フローのエラーハンドリング改善`）
- **PR本文**: すべて日本語で記載（概要・変更詳細・テスト結果など）
- **コミットメッセージ**: 日本語またはConventional Commits形式（概要部を日本語）
- **チャット応答・説明文**: 思考過程のサマリー、計画、報告はすべて日本語
- **専門用語の扱い**:
  - クラス名・関数名・API名・ライブラリ名・エラーコード等の固有名詞はそのまま英語（原文）を使用
  - 説明文・接続詞・解説のみを自然な日本語にする（無理な直訳を避ける）

---

## 3. Pull Request (PR) 作成規約

### 3.1. タイトルフォーマット
以下のプレフィックスを使用し、コロンの後に日本語で簡潔に要約してください。
- `feat:` 新機能の追加
- `fix:` バグ修正
- `refactor:` リファクタリング（機能追加・バグ修正を含まないコード改善）
- `docs:` ドキュメントの更新
- `test:` テストの追加・修正
- `chore:` ビルド・依存関係・設定ファイルの更新

### 3.2. PR本文フォーマット
PR本文は以下の構造を必ず維持してください。

```markdown
## 概要
<!-- 今回の変更の目的と概要を簡潔に記載（1〜3行程度） -->

## 変更内容
<!-- 変更した主な箇所を箇条書きで具体的に記載 -->
- 
- 

## 検証内容・テスト結果
<!-- 実施した動作確認、単体テスト/統合テストのコマンドと実行結果を記載 -->
- 実行コマンド: `yarn lint:all`
- 結果: PASS (X tests passed)

## レビュアーへの確認事項・補足
<!-- 注意点やデザイン上のトレードオフがあれば記載。なければ「特になし」 -->
- 特になし
```

---

## 4. コミット規約 (Commit Guidelines)
- コミットは可能な限り論理的な単位で分割してください。
- コミットメッセージ例:
  - `feat: 認証トークンの自動更新ロジックを実装`
  - `fix: 日付フォーマットのタイムゾーンずれを修正`
  - `test: ユーザー登録APIの境界値テストを追加`

---

## 5. コーディングおよび実装基準 (Coding Standards)
1. **既存パターンの踏襲**: プロジェクト内の既存コードの命名規則、設計パターン、ディレクトリ構造に合わせる。
2. **スコープの限定**: 指示されたタスク以外の無関係なコードのリファクタリングやフォーマット変更を行わない。
3. **安全性の確保**: 破壊的変更（Breaking Changes）を避け、後方互換性を維持する。
4. **テスト駆動・検証**:
   - 変更に関連するテストが存在する場合は必ず実行し、全件パスすることを確認する。
   - 新規機能やバグ修正を行う場合は、可能な限り再現テスト・ユニットテストを追加する。

---

## 6. 禁止事項 (Prohibited Actions)
- 英語でのPR作成やチャット返答（コード内のコメント・コミット規約の型名を除く）
- テストが失敗した状態でのPR作成
- `package.json` / `requirements.txt` / `go.mod` 等への不要な依存関係の無断追加
- 秘密情報（APIキー、トークン、環境変数の実値など）のハードコードおよびコミット
